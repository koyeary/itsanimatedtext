import React, { Fragment } from 'react';
import { Link } from 'react-router-dom';
import Product from '../Product';
import { Popover, OverlayTrigger, Button } from 'react-bootstrap';


const Cart = ( /* {product}  */) => {
  //const cartIcon = <i className='fa fa-shopping-cart' aria-hidden='true' />;
        return (<Button>bitch this is a button</Button>);
/*   const popover = (
    <Popover>
      <Popover.Title>My Cart</Popover.Title>
      <Popover.Content>
{/*       <Product key={product._id} product={product} /> 
            this is very much a test
      </Popover.Content>
    </Popover>
  );

  return (
    <Fragment>
      <OverlayTrigger trigger='click' placement='right' overlay={popover}>
        <Button variant='success'>{cartIcon}</Button>
      </OverlayTrigger>
    </Fragment>
  ); */
};

export default Cart();
 